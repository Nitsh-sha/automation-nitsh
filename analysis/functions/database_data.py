# analysis/functions database_data.py
# The dictionary below is meant for converting names of venues to their proper venue id's when they are being referred to
# Maybe will expand in getting replaced by a query that retrieves all venue ids and is stored into a dictionary on the fly.
venues = {
    'MGM Corporate': 130,
    'MGM Bellagio': 272
}